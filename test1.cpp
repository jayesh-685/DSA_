#include "bits/stdc++.h"
using namespace std;


int main () {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	clock_t z = clock();
	int t = 1, l;
	cin >> t;
	cout << "Run Time : " << ((double)(clock() - z) / CLOCKS_PER_SEC);
	return 0;
}